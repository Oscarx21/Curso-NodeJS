{{#each erros}}

<div class="alert alert-danger">{{texto}}</div>

{{else}}

{{/each}}

<h3>Editar Categoria</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/categorias/edit" method="POST">

        <input type="hidden" name="id" value="{{categoria._id}}">

             <label for="nome">Nome</label>
             <input type="text" value="{{categoria.nome}}" name="nome" class="form-control" placeholder="Nome da Categoria" id="nome"><br>

             <label for="slug">Slug:</label>
             <input type="text" name="slug" value="{{categoria.slug}}" id="slug" class="form-control" placeholder="Slug da Categoria"><br>
        <button type="submit" class="btn btn-success">Salvar categoria</button>
        </form>
</div>

</div>